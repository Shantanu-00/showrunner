rules_version = '2';

// Deny-by-default. Full matrix (consent rings, host claims, subject veto,
// visibility enforcement) lands in B3 (spec 09 §3) alongside rules-tests/.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
