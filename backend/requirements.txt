# Shared service runtime: api · intake · dlq (and the B2 perception workers, which ride the
# same image). The heavy, single-purpose dependency sets are split out deliberately —
# requirements-face.txt (insightface/onnxruntime) and requirements-render.txt (librosa/ffmpeg) —
# because the face probe measured a 1049 MB image with a 29.6 s cold start, and `intake` scales
# 0→20 on a burst. See docs/context/friction-log.md.
fastapi
uvicorn[standard]
pydantic>=2
firebase-admin
google-cloud-firestore
google-cloud-storage
google-cloud-tasks
google-cloud-vision
google-cloud-secret-manager
google-adk>=2.7,<3
google-genai
# Model Armor has no pinned client library; services/armor.py calls its REST surface through
# google-auth's AuthorizedSession, which needs requests. Transitively present via google-api-core —
# declared here because we import it directly.
requests
pillow
pillow-heif
piexif
tzdata
